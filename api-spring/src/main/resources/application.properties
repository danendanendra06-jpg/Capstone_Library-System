spring.datasource.url=jdbc:mysql://localhost:3306/library_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Use validate or none so it does not interfere with CI4 migrations, but update is okay for minimal setup.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

server.port=8081
jwt.secret=librarymanagementsystemcapstoneprojectverysecurekey
